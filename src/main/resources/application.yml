spring:
# DataSource
  datasource:
    url: jdbc:mysql://localhost/socket_kotlin
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
# JPA
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
# Thymeleaf
  thymeleaf:
    cache: false